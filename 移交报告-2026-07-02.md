# Ornith 移交报告（2026-07-02）

## 一句话

**双轨主路径**：GPU `awq_marlin`（权重来自 **GGUF 反推 BF16**，禁止下载 70GB 官方 BF16）+ CPU `ornith-cpu-experts-q6k.gguf`（kt LLAMAFILE）。**GGUF on-the-fly** 为备选（映射/VL 命名/out_proj 激活置换已修）。脚本与工具集中在 **`ktransformers` 公共 fork**。

## 今日完成

| 项 | 说明 |
|----|------|
| GGUF→BF16 全量导出 | `model-gpu-from-gguf.safetensors`，630 keys，~4.6GB |
| `package_gguf_bf16_for_awq` | 单分片 + 子集 index，不读 HF 分片；测试通过 |
| 禁止 70GB 下载 | `download-ornith-bf16.sh` / `run-merge-after-export.sh` 已废弃删除 |
| VL GGUF 加载 | `_checkpoint_name_to_model_param`、`get_out_proj_activation_perm` 对拍 |
| 脚本归仓 | `kt-kernel/scripts/ornith/` + `docs/ornith/` |
| autoawq | 已装 `.venv-public-py312` |
| AVX2 SIGILL | kt-kernel avx2 变体无 zmm 污染（见历史 `移交报告.md`） |

## 未完成 / 接手优先

1. **AutoAWQ + 起服**：`bash kt-kernel/scripts/ornith/run-full-gguf-awq-pipeline.sh` → `run_sglang_marlin_ik.sh`（8GB 可能 OOM）
2. **GGUF 全模型 E2E**：`run_sglang_gguf.sh` — 若仍遇 `UninitializedParameter`/显存，见 sglang `gguf.py` / MoE 物化
3. **IQ2_K_R4**：仅 hybrid CPU 专家需要，`docs/ornith/ik-kt-iq2_k_r4-backfill.md`
4. **多模态 visual**：GPU 导出无 `model.visual.*`；需 mmproj 或官方 visual 权重

## 仓库与推送

| 仓库 | 远程 | 内容 |
|------|------|------|
| `PamelaSprin47685ghall/ktransformers` | `fork` | kt-kernel tools/tests/scripts/docs、移交本文件 |
| `PamelaSprin47685ghall/sglang` | `origin` | qwen35moe GGUF、VL load、测试 |
| `PamelaSprin47685ghall/ik_llama.cpp` | `fork` | ornith 本地 build 脚本（如有） |

工作区 `/home/kunweiz/Desktop/Ornith` **非 git**；权重与 `ornith-*-from-gguf` 目录不入库。

## 环境

- venv：`/home/kunweiz/Desktop/Ornith/.venv-public-py312`
- `pip install -e sglang-fork/python`
- `PYTHONPATH=sglang-fork/python:kt-kernel/python`
- GPU：RTX 3070 Ti 8GB；CPU：AVX2 only

## 测试

```bash
cd sglang-fork/python && pip install -e . && pytest test/srt/test_qwen35*.py -q
cd ktransformers/kt-kernel && pytest test/per_commit/test_package_gguf_bf16_for_awq.py test/per_commit/test_merge_gguf_export_into_hf.py -q
```

## 约束（仍有效）

- 改动在三个公共 fork，不 patch site-packages
- 禁止为省事下载 70GB BF16；用 GGUF 导出 + package
- llama.cpp/kt CPU **仅 CPU 指令集**，本机无 AVX512
- 数值权威：`GGUF_on_the_fly.txt`